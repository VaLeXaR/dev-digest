import type React from "react";

export const s = {
  hint: { fontSize: 13, color: "var(--text-muted)", marginBottom: 20 } as React.CSSProperties,
  surface: {
    background: "var(--bg-surface)",
    borderRadius: 8,
    padding: 24,
    border: "1px solid var(--border)",
    lineHeight: 1.7,
    fontSize: 14,
  } as React.CSSProperties,
};
