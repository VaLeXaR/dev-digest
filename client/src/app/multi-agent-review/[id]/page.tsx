/* Multi-Agent Review results — /multi-agent-review/:id (T-12/T-13).
   Thin page: all logic lives in the colocated ResultsView. */
import { ResultsView } from "./_components/ResultsView/ResultsView";

export default function MultiAgentRunResultsPage() {
  return <ResultsView />;
}
