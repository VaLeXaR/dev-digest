# Retro Ledger

One row per `/workflow-retro` run. Append-only — this is the trend source, don't hand-edit
past rows. **Regression thresholds** (flag in the retro report, don't just log silently):
cost or token total up >20% week-over-week for a comparable workflow type, or fix-loop
rounds trending upward across 3+ consecutive runs of the same kind.

| date | label | workflow | agents (top/nested) | in→out tok | read_ratio | wall | parallelism | cost | fix-loop rounds (arch/plan) | top recommendation |
|------|-------|----------|----------------------|-----------|-----------|------|-------------|------|------------------------------|---------------------|
| 2026-07-09 | project-context-planning | implementation-planner + grilling (spec→plan stage only) | 3/1 | 132.3K→55.4K | 84.5% | 2266.6s | 1.07x | ~$15.53 (planner cost estimated, model tag missing) | n/a | Resume a stalled/API-error agent via SendMessage before treating it as failed and re-dispatching |
| 2026-07-09 | project-context-replan | implementation-planner (full rewrite, post spec-revision) + grilling | 1/n·a (undercounted — see report) | 157.5K total (no in/out split, in-context only) | n/a | 652.0s (planner dispatch only; grilling wall-clock unmeasured) | 1.0x | n/a (not verified this run) | n/a (pre-implementation stage) | Grilling's own codebase-evidence questions must dispatch `researcher` subagents per the skill's explicit instruction, not direct Grep/Read/Bash by the coordinator |
| 2026-07-09 | project-context-implementation | run-plan (full build+verify, 15-task multi-agent) | 21/0 | 1.13M→489K | 94.2% | 27530.9s (7.65h) | 1.63x | n/a (pricing not verified) | 0/1 (arch-reviewer PASS first try; plan-verifier looped 1 round: REVIEW→fix→PASS) | Dispatch a same-phase task as soon as its own `Depends-on` clears, not when the whole phase batch reports — would have saved ~3.8h of this run's wall-clock (T-05 waited behind T-06/T-07's unrelated API-limit stall) |
| 2026-07-10 | onboarding-generator-implementation | run-plan (full build+verify, 8-task multi-agent) | 15/≥3 (undercounted — T-06 +1, T-08 +2 nested live-browser verification agents, exact token cost unknown) | 1.67M total (no in/out split, in-context only) | n/a (deep mode not run) | 3907.2s (~65.1min) | 1.41x | n/a (pricing not verified) | 1/1 (architecture-reviewer: 1 fix round to PASS; plan-verifier: 1 fix round for R25 before final PASS) | Coordinator had the verified-correct fix for a `RepoIntel` interface-typing issue in hand (from T-03's Process notes) before dispatching T-04, but chose "stay consistent with facts.ts's existing typing" over applying the known fix — causing the entire architecture-reviewer fix-iterate round (~139K tokens, ~166s) that a corrected T-04 brief would have avoided |
| 2026-07-11 | why-risk-brief-spec | spec-creator + spec-clarification (spec-authoring stage only, first-of-type baseline) | 1/0 (spec-creator reported no nested dispatch; unconfirmed) | 100.6K total (no in/out split, in-context only) | n/a (deep mode not run) | 278.6s (spec-creator dispatch only; spec-clarification's 10-question interview is user-response-bound, not agent compute) | 1.0x (single serial dispatch, no concurrent agents) | n/a (pricing not verified) | n/a (pre-implementation stage) | Coordinator ran ~20 serial Read/Grep/Glob/Bash calls for pre-dispatch grounding instead of delegating to a parallel `researcher`/`Explore` subagent — likely re-verified by spec-creator anyway per its own evidence rules; no documented digest-handoff pattern exists yet for Researcher → Spec Creator (unlike Pattern 1 for Implementation Planner) |
| 2026-07-11 | why-risk-brief-planning | implementation-planner + grilling (plan-authoring stage only) | 1/0 (implementation-planner reported no nested dispatch in its summary; unconfirmed via deep mode) | 187.5K total (no in/out split, in-context only) | n/a (deep mode not run) | 665.5s (planner dispatch only; grilling's 6-question interview is user-response-bound, not agent compute) | 1.0x (single serial dispatch, no concurrent agents) | n/a (pricing not verified) | n/a (pre-implementation stage) | `implementation-planner`'s frontmatter `tools:` grant omits `Skill` even though its own body (Skill-loading section + Method step 5) instructs it to call that tool per domain — align the grant with the documented behavior, or remove the unreachable instruction |
| 2026-07-12 | why-risk-brief-implementation | run-plan (full build+verify, 10-task multi-agent + 1 fix-loop round) | 16/1 | 666.4K→259.2K | 91.2% | 4704.9s (~78.4min) | 1.02x | ~$47.57 (1 of 17 agents' cost unresolved — journal `model` tag `<synthetic>` didn't match the price map, so true total is somewhat higher) | 0/1 (architecture-reviewer clean PASS first try; plan-verifier: 1 round, REVIEW→fix→PASS on R11) | `workflow-retro`'s `analyze-journals.cjs` fails silently ("no readable journal files matched") against git-bash `/c/...`-style paths passed via the `Bash` tool on Windows — Node's `path.dirname()` resolves them against the wrong drive; must invoke via `PowerShell` with a native `C:\Users\...` path instead |
| 2026-07-17 | skill-eval-code-input | implementation-planner ×2 + grilling + run-plan (full build+verify, 9-task multi-agent, 0 fix-loop rounds) | 16/6 | 77.9K→371.8K | 92.7% | 8329.6s (~2.31h, includes two user interview gaps) | 0.92x overall — but 3.83x within Phase 2's 5-way concurrent batch; the headline figure is diluted by coordinator time and user round-trips, not by serial dispatching | **$57.85** — ⚠️ **+21.6% vs. the comparable 2026-07-12 run ($47.57), crossing the ledger's >20% threshold.** Attribution: $5.33 on two dead T-08 dispatches + $8.29 on a second full planner dispatch for the post-grilling rewrite. Net of those, $52.52 (+10.4%) | 0/0 (both gates PASS first try — best of the 4 run-plan builds logged) | Before dispatching any agent whose task drives `chrome-devtools-mcp`, verify from the coordinator that the browser profile is free and the app is already up (one `list_pages` + one `curl`) — an orphaned Chrome held the profile lock and `./scripts/dev.sh` was in the task's own steps, costing two hung dispatches (1732s, $5.33) before diagnosis |
| 2026-07-19 | export-to-ci-implementation | run-plan (full build+verify, 5-task multi-agent + 1 fix-loop round) | 11/6 (deep; plan-verifier S2 fanned out 6 `researcher`s) | in 2.1K→out 308.2K (+113.3M cache-read) | 95% | 9567.6s (~2.66h; sum_agent_span 6440.9s) | 0.67x overall (diluted by serial verify chain + coordinator time; higher per-phase) | $70.15 (deep, nested-incl., verified prices 2026-06-24) | 0/1 (architecture-reviewer PASS first try; plan-verifier 1 round: REVIEW→AC-39 fix→PASS) | When a task adds an agent-editor tab, `implementation-planner` must add the parent route's tab-gate file (`app/agents/[id]/page.tsx` `VALID_TABS`) to that task's Owned paths — else the tab is unreachable with green tests and no implementer owns the fix |
| 2026-07-19 | multi-agent-review-implementation | run-plan (full build+verify, 14-task multi-agent, cost-conscious tail: arch-reviewer→plan-verifier, no early functional pass) | 23/4 (19 top-level implementers/reviewers + 4 nested `researcher` under `plan-verifier`; T-08's nested live-browser `general-purpose` agent also captured) | 215.2M in-side (206.5M cache-read + 8.73M cache-write + 3.7K fresh)→464.0K out | 96% | 10431.7s (~2.9h, incl. one ~61-min user round-trip between Wave 5 and Wave 6) | 1.08x overall — but 1.8–2.3x within each 3-way implementer wave (Wave 5 T-10∥T-11∥T-12 = 2.31x); headline collapsed by a single ~61-min idle gap (user "продовжуй" nudge), not serial dispatching | **≈$169 notional** (Opus 4.8 rates: in $5 / out $25 / cache-read $0.50 / cache-write $6.25 per M; billed $0 — subscription run). Dominated by cache-read $103 + cache-write $55 (93%); output only $11.60. Caching saved ~$929 vs. uncached input. `analyze-journals.cjs --prices` under-priced to $12.57 because 19 of 23 journals tag model `<synthetic>` (known matcher gap) — computed from correct aggregate token totals. Output **464K tok, +24.8% vs. 2026-07-17 (372K)** crosses the >20% token threshold but is **scope-driven**: 14 tasks vs. 9; per-task output (33.1K) sits *between* the two prior builds (25.9K, 41.3K) — no per-task efficiency regression | 1/0 (architecture-reviewer: 1 fix round — 3 highs, all one root-cause `shared-component-route-coupling` → PASS; plan-verifier: 0 rounds, clean final gate) | Scope live-browser design verification to task complexity — a one-line nav/UI change should be checked structurally, never by spawning a 582s live-browser design subagent (T-08 did, with a wrong-worktree near-miss), while the complex results/configure/picker tasks deferred theirs — effort inverted against risk |
