# Workflow Insights

Process-level retrospective log for multi-agent SDD/run-plan runs — friction, near-misses,
and process recommendations. Companion to per-module `INSIGHTS.md` (codebase knowledge)
and to `docs/retros/ledger.md` (the quantitative trend row for the same runs — this file
is the qualitative "why", that file is the "how much").

> Entries are LLM-generated inference from a single run's transcript and self-reported
> `Process notes`, not independently measured ground truth — a human spot-check is
> expected, same as `INSIGHTS.md`.

## Friction Points

- 2026-07-09: `implementation-planner`'s first dispatch leg stalled mid-stream with an API-level error (`"Response stalled mid-stream"`) right before writing the plan file, surfacing as a `status: failed` task-notification — not a reasoning failure, and it recovered cleanly once resumed. (`implementation-planner`)
- 2026-07-09: During the `project-context` `run-plan` build, 4 different `implementer` dispatches (T-06, T-07, T-13, T-14) each failed once mid-run with an account-level API session-limit error, unrelated to task content — all 4 recovered cleanly on a single `SendMessage` resume each, but two of them (T-06, T-07) had already been idle for ~3.85h before the resume actually succeeded (the stated "resets 6:30pm" limit reset had not yet occurred despite the resume call appearing to accept immediately). (`implementer` T-06/T-07/T-13/T-14)
- 2026-07-09: One `implementer` (T-14, Attached Specs modal) stalled twice in a self-inflicted loop: it launched its own verification test with `run_in_background`, then ended its turn with text like "I'll pause here and wait for the background test-run notification before finalizing my report" — but subagents don't receive task-notifications the way the orchestrator does, so nothing was coming. Needed 2 coordinator interventions (the second an explicit "run this synchronously, do not background it, do not end your turn without a final report") before it produced a DONE. (`implementer` T-14)

## What Worked Well

- 2026-07-09: Coordinator invoked `grilling` immediately on `implementation-planner`'s `**Next step:**` directive (Pattern 5, `.claude/agents/README.md:559`), and grilling's dependency-tree interview surfaced 3 unconfirmed risks (version-bump skipping, in-memory discovery cache, missing test-writer) that the plan itself hadn't flagged for confirmation, alongside the 1 it had. (`implementation-planner` → `grilling`)
- 2026-07-09: Two `researcher` subagents dispatched in one message for independent grilling questions ran concurrently (52s / 60s spans, ~6s apart) and each returned a ~250-300 word digest despite consuming 300-450K cache-read tokens internally — the coordinator's context only absorbed the digest. (`researcher` x2)
- 2026-07-09: On a full plan rewrite triggered by a substantially revised spec, the coordinator pre-synthesized the spec diff (what changed structurally — removed DB tables, `origin` field, new tracked-status concept — and why) directly into the `implementation-planner` dispatch prompt instead of leaving the planner to re-derive it from a raw `git diff`. The planner's own Process note confirmed the effect: "the rewrite brief pre-supplied the spec-diff context, so verification was fast." (coordinator → `implementation-planner`)
- 2026-07-09: On the `project-context` `run-plan` build, the coordinator's own security fix (a path-traversal scoping check) was itself found incompletely correct by a *third*, narrowly-scoped `plan-verifier` re-dispatch — asked specifically to trace `..`-crafted inputs through the new code rather than re-run the full 23-requirement sweep. That targeted re-check used less than a third of the tokens/time of the original functional pass (42.3K in / 118s vs. 57.4K in / 422s) while catching a real bug the first two passes both missed. Confirms a cheap, narrowly-scoped re-verification dispatch is the right tool once a specific fix needs confirming — don't default to a full re-sweep. (coordinator → `plan-verifier`)
- 2026-07-09: All 4 `SendMessage` resumes of infra-failed `implementer` agents on the `project-context` build succeeded cleanly (0 wasted re-dispatches) — the single-agent pattern logged in this file's Friction/Near-Misses sections from the planning stage held at 4x scale in the same session. (`implementer` x4)

## Duplicated / Wasted Work

- 2026-07-09: `implementation-planner` had its nested `Explore` subagent map client integration points, then re-verified those findings itself before citing them in the plan — modest duplication, but a deliberate anti-hallucination check rather than wasted effort. (`implementation-planner` → `Explore`)
- 2026-07-09: On the `project-context` `run-plan` build, the coordinator dispatched T-05 only after the entire Phase-2 batch (T-04, T-06, T-07) reported back, even though T-05's plan-stated `Depends-on` was T-04 alone (T-05 was sequenced after T-04 only because both own the same module directory). T-04 finished in ~7 minutes; T-06/T-07 then stalled on an unrelated API session-limit for ~3.85 hours. T-05 sat fully ready and undispatched for that entire window — the single largest wall-clock cost in the run, and structurally avoidable: nothing about T-05 required T-06/T-07 to finish first. (coordinator dispatch scheduling)
- 2026-07-09: Three separate `implementer` dispatches (T-11, T-12, T-13) independently used `pnpm test -- <filter>` — a footgun where pnpm forwards a literal `"--"` into `vitest run`, which hangs instead of filtering — leaving ~166 orphaned `node.exe`/tinypool worker processes running for 90+ minutes on the shared dev machine. This starved CPU for every later dispatch, including the coordinator's own `pnpm typecheck`/`pnpm test` verification runs, and was only caught when the user asked why a run was taking unusually long. Now fixed going forward (documented as an `AGENTS.md` project-wide gotcha), but cost real coordinator+user time to diagnose and clean up (166 processes killed) mid-run. (`implementer` T-11/T-12/T-13)

## Near-Misses

- 2026-07-09: A `SendMessage` resume recovered `implementation-planner` after its API-level mid-stream stall; without the resume, the ~35 minutes and tokens already spent on that dispatch would have been lost and the plan file would never have been written. (`implementation-planner`)
- 2026-07-09: In a follow-up `grilling` pass on a rewritten plan, the coordinator answered 2 codebase-evidence questions (checking for a Redis dependency; checking `simple-git.ts` for an existing per-clone lock) via direct `Grep`/`Read`/`Bash` calls instead of dispatching `researcher` subagent(s), contrary to `grilling`'s own `SKILL.md` instruction ("dispatch a researcher subagent via Agent rather than reading files directly yourself") and a regression from the prior run's logged good pattern (2 parallel `researcher` dispatches for grilling questions, see What Worked Well above). No harm this time — both checks were single-fact lookups answered correctly — but the pattern would degrade on a larger/more ambiguous evidence question. (coordinator → `grilling`)
- 2026-07-09: A real security bug (a path-traversal scope-check bypassable via a `..`-crafted path, also defeating a pre-existing tracked-file edit gate) almost shipped silently: the functional `plan-verifier` pass didn't catch it (out of its requirement-coverage scope), and `architecture-reviewer` found the *missing* check but reported it as a prose "process note" rather than a gated finding — a coordinator that only reads the Gate verdict / findings table (not the full prose report) would have missed it entirely. It was only caught because the coordinator fixed it anyway and then asked a third `plan-verifier` pass to specifically re-trace the fix. (`architecture-reviewer` → coordinator → `plan-verifier`)

## Process Recommendations

- 2026-07-09: On a `failed` task-notification from an async agent, check whether the cause is a stall/API-error (as opposed to a genuine task-level failure) and attempt one `SendMessage` resume before re-dispatching from scratch — this run's resume succeeded and avoided re-incurring ~$10 and 35 minutes. (orchestrator behavior)
- 2026-07-09: Standardize future `researcher` dispatch prompts on the literal `output: compact-digest` directive from Pattern 1 (`.claude/agents/README.md:530`) instead of ad hoc "under N words" phrasing, so digest-sizing is auditable against the documented pattern. (dispatch-prompt convention)
- 2026-07-09: During `grilling`, dispatch `researcher` subagent(s) for codebase-evidence questions per the skill's own instruction rather than defaulting to direct `Grep`/`Read`/`Bash` — the skill currently has no proportionality carve-out for a trivial single-fact check, so either follow it consistently or propose (as a separate, explicitly-approved change) adding an explicit threshold to `grilling/SKILL.md` for when a direct check is acceptable. (`grilling` skill adherence)
- 2026-07-09: After `implementation-planner` returns a `**Next step:**` directive naming a mandatory handoff (e.g. run `grilling`), invoke it immediately rather than pausing to ask the requester for permission — `CLAUDE.md`'s "Mandatory handoffs — do not skip, even mid-conversation" already pre-authorizes the action; asking first only adds an avoidable round-trip for something that isn't actually discretionary. (orchestrator behavior)
- 2026-07-09: `run-plan`'s Stage 1 dispatch model gates on whole-phase completion ("A phase is complete only when every task in it reports DONE... Do not start a dependent phase early"), but within a single phase containing multiple parallel tasks, a task that depends on only ONE sibling (per its own `Depends-on` line) should be dispatched the moment that specific sibling clears — not held until every other sibling in the same phase also reports back. On the `project-context` build this cost ~3.8h of wall-clock (T-05 depended only on T-04, but wasn't dispatched until T-06 and T-07 — both stalled on an unrelated API limit — also finished). Track per-task dependency satisfaction within a phase, not phase-batch completion, when a same-phase task's `Depends-on` doesn't cover every task dispatched alongside it. (`run-plan` skill, Stage 1)
- 2026-07-09: `implementer` agents should be told explicitly not to launch their own verification command with `run_in_background` and then end their turn expecting a notification — subagents don't receive the orchestrator's task-notification mechanism, so that pattern stalls indefinitely until the coordinator notices and intervenes (observed twice in one dispatch, see Friction Points). Add to the implementer definition: run verification synchronously, or if a long-running command must be backgrounded, actively poll and read its result before ending the turn. (`implementer` agent definition)
