# Workflow Insights

Process-level retrospective log for multi-agent SDD/run-plan runs — friction, near-misses,
and process recommendations. Companion to per-module `INSIGHTS.md` (codebase knowledge)
and to `docs/retros/ledger.md` (the quantitative trend row for the same runs — this file
is the qualitative "why", that file is the "how much").

> Entries are LLM-generated inference from a single run's transcript and self-reported
> `Process notes`, not independently measured ground truth — a human spot-check is
> expected, same as `INSIGHTS.md`.

## Friction Points

- 2026-07-09: `implementation-planner`'s first dispatch leg stalled mid-stream with an API-level error (`"Response stalled mid-stream"`) right before writing the plan file, surfacing as a `status: failed` task-notification — not a reasoning failure, and it recovered cleanly once resumed. (`implementation-planner`)
- 2026-07-09: During the `project-context` `run-plan` build, 4 different `implementer` dispatches (T-06, T-07, T-13, T-14) each failed once mid-run with an account-level API session-limit error, unrelated to task content — all 4 recovered cleanly on a single `SendMessage` resume each, but two of them (T-06, T-07) had already been idle for ~3.85h before the resume actually succeeded (the stated "resets 6:30pm" limit reset had not yet occurred despite the resume call appearing to accept immediately). (`implementer` T-06/T-07/T-13/T-14)
- 2026-07-09: One `implementer` (T-14, Attached Specs modal) stalled twice in a self-inflicted loop: it launched its own verification test with `run_in_background`, then ended its turn with text like "I'll pause here and wait for the background test-run notification before finalizing my report" — but subagents don't receive task-notifications the way the orchestrator does, so nothing was coming. Needed 2 coordinator interventions (the second an explicit "run this synchronously, do not background it, do not end your turn without a final report") before it produced a DONE. (`implementer` T-14)
- 2026-07-10: `implementer` T-08 (Onboarding Tour page + 5 section components + i18n) ran for 38.2 minutes (272K tokens, 168 tool calls) with zero visible transcript-file activity for the last ~30 min of that window, prompting the user to ask twice whether the agent had hung. The harness's own `TaskOutput` status check confirmed `status: running` throughout, and the delay was legitimate — two nested live-browser verification subagents catching and fixing two real scroll-spy bugs. No actual hang, but the task's sheer size made "still running vs. stuck" genuinely hard to tell from outside. (`implementer` T-08)
- 2026-07-11: `implementation-planner`'s own Process note reported "the Skill tool was unavailable in this context" during the `why-risk-brief` plan dispatch. Its frontmatter `tools:` grant (`Read, Glob, Grep, Bash, Agent, WebFetch, Write`) has never included `Skill`, despite the agent body's extensive "Skill loading (lazy, by domain)" section and Method step 5 both directing it to call the `Skill` tool per touched domain. It degraded gracefully — referenced skills by name from verified codebase conventions instead of loading actual skill content — but this is a structural contradiction in the agent definition, not a one-off. (`implementation-planner`)
- 2026-07-12: `workflow-retro`'s `deep` mode `analyze-journals.cjs` failed silently with "no readable journal files matched" when the glob path was passed in git-bash POSIX style (`/c/Users/.../subagents/agent-*.jsonl`) from the `Bash` tool — Node on Windows resolves that as a relative path against the wrong drive root, not `C:\Users\...`. Retrying via the `PowerShell` tool with a native `C:\Users\...` path succeeded immediately, no code change needed. (`workflow-retro` analyzer script, Windows-specific)
- 2026-07-12: `implementer` T-06 (structured LLM extractor, `why-risk-brief` build) took 866s — 2-4x every sibling implementer task in the same run despite comparable token/tool-call counts — and its journal's `model` field logged `<synthetic>` instead of `claude-sonnet-5`, breaking cost estimation for that one dispatch. T-06 itself self-reported a clean first-pass success (9/9 tests, no red-then-green iteration), so the extra wall-clock and the odd model tag are unexplained from the in-context report alone. (`implementer` T-06)

- 2026-07-17: Two consecutive `implementer` T-08 dispatches (live design-fidelity pass) hung and were interrupted by the user, and **both had already run** by the time the rejection landed — a rejected tool-use call is not proof the agent never executed (the first got far enough to create a stray `t08-design-check` eval case in the live DB, which the coordinator later had to clean up). Root cause was environmental, not agent-side, and neither agent could report it: attempt 1 left an orphaned `chrome-devtools-mcp` Chrome process holding the profile lock, so every subsequent MCP call failed to attach ("The browser is already running for …chrome-profile"), and the task's own step 1 instructed the agent to run `./scripts/dev.sh` — a foreground process that never exits. The user saw an open browser and a silent agent; `rtk ps` does not list Windows GUI processes, so diagnosis needed `Get-CimInstance Win32_Process`. (`implementer` T-08 ×2)

- 2026-07-17: **SUPERSEDES the entry directly above on one load-bearing point: neither T-08 dispatch actually hung.** Written before checking the journals; the journals say otherwise. Attempt 1's nested agent made **33 tool calls in 9.4 minutes**, its longest gap was 122s, and its final `Grep` lands on the same second as the interrupt — it was working throughout. Attempt 2's nested agent hit `The browser is already running for …chrome-profile` on four separate MCP calls (`navigate_page`, `list_pages`, `new_page`, `take_snapshot`), then ran three PowerShell probes at 10:45:10–10:45:34 — i.e. it had **detected the orphaned-Chrome blocker and was diagnosing it**, the same diagnosis the coordinator performed manually 10 minutes later. Both were killed mid-work, not mid-hang. The environment-check recommendation still stands (the blocker was real and would have cost the agent time regardless), but "the agent hung" was a coordinator assumption, never verified — and it is exactly the kind of unverified diagnosis this file exists to catch. (`implementer` T-08 ×2 — correction)

- 2026-07-19: On the `multi-agent-review` build the coordinator yielded its turn after Wave 5 (posting a progress summary) at an **intra-implementation wave boundary — not a designed gate** — and the run sat idle ~61 min until the user typed "продовжуй" to resume it into Wave 6/T-13. That single gap is the entire reason deep-mode's headline `parallelism` read 1.08x; per-wave concurrency was healthy at 1.8–2.3x (Wave 5's T-10∥T-11∥T-12 = 2.31x, all three started within 35s). A new cause for the already-logged parallelism-dilution caveat (2026-07-17): here the diluter was a user round-trip *mid-implementation*, and it was avoidable — Wave→Wave is not a checkpoint, only the post-build gates are. (`run-plan` Stage 1, coordinator)
- 2026-07-19: During an interactive `spec-clarification` interview, the engineering-insights **stop-gate re-fires on EVERY answered question** — because each resolved item edits the one spec file (`Edit`), and the gate keys off "files changed since last check". Its own message ("This gate will not fire again for these files") is misleading in this loop: the next answer re-edits the same file, producing a new change set that re-arms the gate. On the `multi-agent-review` spec run this meant a mandatory insights-gate turn between every single Q&A exchange (~8 items). It is not a malfunction, but it is friction: the coordinator must either emit a one-line "nothing passes the filter" each turn or batch several spec edits before yielding. Real insights DID surface on a couple of those turns (sequential executor → `server/INSIGHTS.md`; non-reusable matcher → `reviewer-core/INSIGHTS.md`), so the gate can't simply be ignored — read each fire, record if substantive, else one-line-and-stop. (`spec-clarification` + `insights-stop-gate.sh`)

## What Worked Well

- 2026-07-17: The 2026-07-10 task-sizing recommendation landed and worked. `implementation-planner` flagged T-07 as an **`Expected-long task`** in its own gotchas ("Do not read a long runtime as a hang") and split its live design verification out into a separate T-08 — exactly the "split build from design-verify" fix logged after `onboarding-generator`'s T-08. T-07 still ran 1072.2s / $5.57 as the run's single longest dispatch and its whole critical path, but nobody asked whether it had stalled. (`implementation-planner` task sizing)
- 2026-07-17: Zero fix-loop rounds — `plan-verifier` (20/21 VERIFIED, 1 accepted PARTIAL) and `architecture-reviewer` (0 findings across all 12 rules) both PASSed on the first dispatch. Best result across the four `run-plan` builds logged here (prior: 0/1, 1/1, 0/1). The two gates were dispatched as one **parallel pair** and Stage 5 was skipped entirely, since neither produced a fix that could invalidate the other: $8.70 combined vs. the prior run's $10.05 for Stage 5 alone. The tradeoff is real and worth naming — running Stage 2 and Stage 3 concurrently forfeits the `## Architecture review: PASS` skip-signal that `run-plan` Stage 5 is designed to consume. (`run-plan` Stages 2/3/5)
- 2026-07-17: `plan-verifier` reached for diff-stat as evidence instead of interpretive reading: to confirm R9's "`executeSkillCase` only, `executeCase` untouched" boundary it cited `git diff` on `service.ts` being **+28/−0** — zero deletions is arithmetic proof the agent path is byte-untouched. Its own Process note called this out as the cheapest decisive check of the run. Worth reaching for whenever a requirement is of the form "X changed, Y provably didn't". (`plan-verifier`)
- 2026-07-17: The live design-fidelity pass paid for itself on its first use. 12 `SkillInputPanes` unit tests were green while the Before/After fields rendered the literal string `eval.caseEditor.codeTab.beforePlaceholder` on screen — the placeholder values are TypeScript snippets containing `{`/`}`, which next-intl parses as ICU argument syntax, so the message failed to parse and fell back to printing the key path. The existing tests asserted only the PR-meta placeholders, which are brace-free and structurally could not catch it. (coordinator T-08, live verification)
- 2026-07-09: Coordinator invoked `grilling` immediately on `implementation-planner`'s `**Next step:**` directive (Pattern 5, `.claude/agents/README.md:559`), and grilling's dependency-tree interview surfaced 3 unconfirmed risks (version-bump skipping, in-memory discovery cache, missing test-writer) that the plan itself hadn't flagged for confirmation, alongside the 1 it had. (`implementation-planner` → `grilling`)
- 2026-07-09: Two `researcher` subagents dispatched in one message for independent grilling questions ran concurrently (52s / 60s spans, ~6s apart) and each returned a ~250-300 word digest despite consuming 300-450K cache-read tokens internally — the coordinator's context only absorbed the digest. (`researcher` x2)
- 2026-07-19: A cross-cutting environment blocker was resolved ONCE at the coordinator level instead of N times by agents. All three Wave-2 implementers (T-01/T-02/T-03) independently reported ~7 phantom `server tsc` errors they correctly attributed to a missing `reviewer-core/node_modules` (fresh worktree, no-hoisting monorepo). The coordinator ran `cd reviewer-core && pnpm install` once between waves, reached a clean `tsc: No errors found`, and every later server dispatch then saw an accurate gate — rather than 10+ server-touching agents each re-flagging or working around the same phantom set. Reading the *converging* "not-mine" attributions across parallel agents as one shared root cause (not three separate concerns) is the move. (coordinator, `run-plan` Stage 1)
- 2026-07-09: On a full plan rewrite triggered by a substantially revised spec, the coordinator pre-synthesized the spec diff (what changed structurally — removed DB tables, `origin` field, new tracked-status concept — and why) directly into the `implementation-planner` dispatch prompt instead of leaving the planner to re-derive it from a raw `git diff`. The planner's own Process note confirmed the effect: "the rewrite brief pre-supplied the spec-diff context, so verification was fast." (coordinator → `implementation-planner`)
- 2026-07-09: On the `project-context` `run-plan` build, the coordinator's own security fix (a path-traversal scoping check) was itself found incompletely correct by a *third*, narrowly-scoped `plan-verifier` re-dispatch — asked specifically to trace `..`-crafted inputs through the new code rather than re-run the full 23-requirement sweep. That targeted re-check used less than a third of the tokens/time of the original functional pass (42.3K in / 118s vs. 57.4K in / 422s) while catching a real bug the first two passes both missed. Confirms a cheap, narrowly-scoped re-verification dispatch is the right tool once a specific fix needs confirming — don't default to a full re-sweep. (coordinator → `plan-verifier`)
- 2026-07-09: All 4 `SendMessage` resumes of infra-failed `implementer` agents on the `project-context` build succeeded cleanly (0 wasted re-dispatches) — the single-agent pattern logged in this file's Friction/Near-Misses sections from the planning stage held at 4x scale in the same session. (`implementer` x4)
- 2026-07-10: The prior run's logged fix ("dispatch a same-phase task as soon as its own `Depends-on` clears, not when the whole phase batch reports") held in practice on the `onboarding-generator` build — T-07 was dispatched the moment T-01 completed, without waiting for T-02 or T-06 (its unrelated Phase-1 siblings) to also finish. (`run-plan` Stage 1 dispatch scheduling)
- 2026-07-10: The scoped-re-verification pattern (cheap, narrowly-scoped re-check over a full re-sweep) was used twice on the `onboarding-generator` build and both times matched the ratio previously logged for this pattern: the R25 `plan-verifier` re-check used 53.5K tokens/4 tool calls/48.9s vs. the original functional pass's 142.9K/35/301.4s, and the `architecture-reviewer` round-2 scoped re-audit used 57.6K tokens vs. round 1's 113.6K — roughly half, despite each catching genuinely new evidence rather than rubber-stamping. (`plan-verifier`, `architecture-reviewer`)
- 2026-07-10: Both cross-agent chaining directives were applied correctly on the same run: `## Architecture review: PASS` was forwarded into the final `plan-verifier` gate dispatch (letting it skip re-checking layering/DI/contract-sync), and `## Architecture context:` (a summary of round-1 findings) was reused in the `architecture-reviewer` round-2 dispatch instead of re-deriving CLAUDE.md context from scratch. (coordinator → `plan-verifier`, `architecture-reviewer`)
- 2026-07-11: On the `why-risk-brief` spec run, the coordinator's heavy up-front grounding (found a real `pr_brief`/`risk_brief` naming collision with an already-shipped Risks feature, plus the producing module for Smart Diff groups) was handed to `spec-creator` in a single dispatch prompt, and the agent needed zero clarifying round-trips — it returned a complete, collision-aware spec on the first pass, and the subsequent `spec-clarification` interview saw 9 of 10 open items resolved by accepting the spec's own recommendation (only 1 explicit override). High-quality upfront context correlates with a clean one-shot agent return and a low-friction clarification pass. (coordinator → `spec-creator` → `spec-clarification`)
- 2026-07-11: Pattern 6 (Spec Creator → spec-clarification handoff, `.claude/agents/README.md:593`) was followed correctly and immediately — `spec-creator` ended with its `**Next step:**` directive and the coordinator invoked `spec-clarification` in the very next turn, no permission round-trip. (coordinator → `spec-creator` → `spec-clarification`)
- 2026-07-11: Grilling's proportionality carve-out (added per the 2026-07-09 Process Recommendation — "a single-fact, single-tool-call check... may be answered with one direct call instead of a subagent dispatch") is live in `grilling/SKILL.md` and was used correctly on the `why-risk-brief` plan's grilling pass: the coordinator read both design mockups directly (image content isn't delegable to a text-digest subagent) and one direct `Grep` confirmed an existing empty-state pattern, without dispatching `researcher` for either. Confirms the prior recommendation landed and resolved the recurring friction logged 2026-07-09 (x2). (`grilling`)
- 2026-07-11: `implementation-planner` correctly refused to invent styling for two design elements (`what`/`why`/`risk_level` header, `risks[]` list) that the source spec's mockup didn't visually ground, flagging both as explicit GAPs in a `## Design audit` table instead of guessing — exactly what `CLAUDE.md`'s design-asset-pipeline rule and the agent's own Method step 2 require. Both GAPs were then resolved cleanly in a single `grilling` pass with zero re-asks. (`implementation-planner` → `grilling`)
- 2026-07-11: `implementation-planner` proactively caught a correctness risk before any code was written — recommending `llm.complete()` + manual `safeParse` over `completeStructured()`, citing a specific `server/INSIGHTS.md` (2026-06-22) entry that `completeStructured()` throws on schema mismatch, which would have broken the spec's AC-8 "never 5xx" contract had the more obviously-named `completeStructured()` API been used by default. (`implementation-planner`)
- 2026-07-12: On the `why-risk-brief` `run-plan` build, all 10 implementer tasks — including the plan's own flagged highest-risk task (T-07: service + routes + settings-accessor wiring) — returned `DONE` on the first dispatch, with zero `NEEDS_CONTEXT`/`BLOCKED` round-trips across the whole run. The coordinator threaded each completed task's exact Process-notes signatures (contract field names, function signatures, repository method names) into every dependent task's dispatch brief, per the standing "apply Process notes forward" rule — no dependent task had to guess an upstream shape. (`run-plan` Stage 1)
- 2026-07-12: When `plan-verifier`'s functional pass surfaced a genuine two-reading ambiguity baked into the plan itself (R11's literal AC text said risk file-refs should link to GitHub; the plan's own Design-audit note said plain-text chips instead), the coordinator escalated via `AskUserQuestion` rather than silently picking an interpretation, and dispatched the unambiguous half of the fix (the `resolvableRefs` set bug) in parallel with asking, instead of blocking the entire fix-loop on the pending answer. (coordinator → `AskUserQuestion` → `implementer`)
- 2026-07-12: Cache efficiency across all 17 dispatches (16 top-level implementer/verifier agents + 1 nested screenshot subagent) was strong for a run with no shared parent context between dispatches: 91.2% read_ratio, 91.9% hit_rate, on 78.9M cache-read tokens against only 666K fresh input tokens, with no dispatch showing near-zero cache reads. (deep-mode metrics, this run)

## Duplicated / Wasted Work

- 2026-07-09: `implementation-planner` had its nested `Explore` subagent map client integration points, then re-verified those findings itself before citing them in the plan — modest duplication, but a deliberate anti-hallucination check rather than wasted effort. (`implementation-planner` → `Explore`)
- 2026-07-09: On the `project-context` `run-plan` build, the coordinator dispatched T-05 only after the entire Phase-2 batch (T-04, T-06, T-07) reported back, even though T-05's plan-stated `Depends-on` was T-04 alone (T-05 was sequenced after T-04 only because both own the same module directory). T-04 finished in ~7 minutes; T-06/T-07 then stalled on an unrelated API session-limit for ~3.85 hours. T-05 sat fully ready and undispatched for that entire window — the single largest wall-clock cost in the run, and structurally avoidable: nothing about T-05 required T-06/T-07 to finish first. (coordinator dispatch scheduling)
- 2026-07-09: Three separate `implementer` dispatches (T-11, T-12, T-13) independently used `pnpm test -- <filter>` — a footgun where pnpm forwards a literal `"--"` into `vitest run`, which hangs instead of filtering — leaving ~166 orphaned `node.exe`/tinypool worker processes running for 90+ minutes on the shared dev machine. This starved CPU for every later dispatch, including the coordinator's own `pnpm typecheck`/`pnpm test` verification runs, and was only caught when the user asked why a run was taking unusually long. Now fixed going forward (documented as an `AGENTS.md` project-wide gotcha), but cost real coordinator+user time to diagnose and clean up (166 processes killed) mid-run. (`implementer` T-11/T-12/T-13)
- 2026-07-10: `implementer` T-06 dispatched its own nested subagent to screenshot-verify the new sidebar nav item, but had to check it against an unrelated existing page (`/repos/:repoId/pulls`) since the real `/repos/:repoId/onboarding` route didn't exist yet (that route is T-08's job, dispatched separately). `implementer` T-08 then independently re-verified the same sidebar item as part of its own comprehensive live-browser check once the real route existed — T-06's active-state styling check was explicitly out of scope for its own screenshot by its own admission, and got fully covered by T-08 anyway. Modest duplicated verification effort. (`implementer` T-06, T-08)
- 2026-07-11: Before dispatching `spec-creator` for the `why-risk-brief` spec, the coordinator ran ~20 direct `Read`/`Grep`/`Glob`/`Bash` calls itself to ground the request (finding the `pr_brief`/`risk_brief` collision, `VerdictBanner`, Smart Diff's producing module, etc.) instead of delegating to a `researcher`/`Explore` subagent, contrary to the standing instruction that broad exploration beyond ~3 queries should be delegated. `spec-creator`'s own Hard Rules require it to independently re-verify every `[reused]`/`[deterministic]` citation with its own `file:line` read regardless of what the dispatch prompt claims — so most of that grounding was very likely re-done inside the 100.6K-token, 21-tool-call spec-creator dispatch that followed. No `researcher → spec-creator` digest-handoff pattern is documented (Pattern 1 covers Researcher → Implementation Planner only). (coordinator → `spec-creator`)

- 2026-07-17: The two hung T-08 dispatches burned **1732.4s of agent span and $5.33** across 4 agents (2 top-level `implementer` + 2 nested `general-purpose` each spawned to do the screenshot check) and produced nothing but a stray DB row. The nested agents are where the time actually went — attempt 1's child ran 566s of its parent's 808s. Both dispatches were textually near-identical; the second was re-sent with an anti-hang preamble but the same orphaned-Chrome blocker was still in place, so re-dispatching before diagnosing the environment bought a second identical failure. (`implementer` T-08 ×2 + 2 nested `general-purpose`)
- 2026-07-19: Effort was allocated inversely to task complexity across the UI tasks. T-08 — a ONE-LINE nav entry, the lowest-risk task in the run — spawned a nested live-browser `general-purpose` design-check subagent that cost **582s + 7.74M cache-read + 14.2K output**, nearly doubling T-08's real footprint and *entirely invisible to in-context `<usage>` totals* (only deep-mode surfaced it), to visually confirm a single sidebar label. Meanwhile the four genuinely complex Wave-5/6 UI tasks (results page, configure page, PR picker, tabs) deferred their live design checks to structural JSX-vs-PNG comparison. A nav-label / one-line UI change should get a structural check, never a live-browser subagent — the cheap task over-spent while the expensive ones under-verified. (`implementer` T-08 + nested `general-purpose`)
- 2026-07-19: T-00 ran ALONE in Wave 1 for 575s while T-01/T-02/T-03 — all `Depends-on: none` with non-overlapping owned paths (shared contracts / schema / executor) — waited for the next wave. Under the plan's own MAX-3 concurrency cap, T-00 + two of them could have co-run, trimming up to ~575s of serial prefix. The grilled plan deliberately isolated T-00 because it *moves* the shared FindingCard/RunTraceDrawer folders, but that race rationale doesn't touch the contracts/schema/executor tasks, which don't import those components — the per-task `Depends-on` rule (already logged 2026-07-09/07-10) would have started them alongside T-00. Following the plan's `### Wave` labels over per-task `Depends-on` re-introduced the exact serialization that rule exists to remove. (`run-plan` Stage 1 dispatch scheduling)

## Near-Misses

- 2026-07-17: A requirement was one dispatch away from shipping half-implemented, and only the agent's own honesty caught it. `implementer` T-07's Process notes disclosed a deliberate scope cut — it had not wired R7's abort signal into the seed-mode preview run (`usePreviewEvalRunFromFinding`) because an existing test pinned that call's exact argument shape, and no T-07 acceptance criterion exercised the path. R7 names that hook explicitly. The coordinator read the note and dispatched a targeted fix ($1.35 / 153.6s) *before* the gates ran; `plan-verifier` subsequently scored R7 VERIFIED. Had the note been omitted — it is an optional field — the gap would have reached the merge gate at best, and shipped at worst. Reading Process notes for self-reported scope cuts is not optional coordinator work. (`implementer` T-07 → coordinator)
- 2026-07-09: A `SendMessage` resume recovered `implementation-planner` after its API-level mid-stream stall; without the resume, the ~35 minutes and tokens already spent on that dispatch would have been lost and the plan file would never have been written. (`implementation-planner`)
- 2026-07-09: In a follow-up `grilling` pass on a rewritten plan, the coordinator answered 2 codebase-evidence questions (checking for a Redis dependency; checking `simple-git.ts` for an existing per-clone lock) via direct `Grep`/`Read`/`Bash` calls instead of dispatching `researcher` subagent(s), contrary to `grilling`'s own `SKILL.md` instruction ("dispatch a researcher subagent via Agent rather than reading files directly yourself") and a regression from the prior run's logged good pattern (2 parallel `researcher` dispatches for grilling questions, see What Worked Well above). No harm this time — both checks were single-fact lookups answered correctly — but the pattern would degrade on a larger/more ambiguous evidence question. (coordinator → `grilling`)
- 2026-07-09: A real security bug (a path-traversal scope-check bypassable via a `..`-crafted path, also defeating a pre-existing tracked-file edit gate) almost shipped silently: the functional `plan-verifier` pass didn't catch it (out of its requirement-coverage scope), and `architecture-reviewer` found the *missing* check but reported it as a prose "process note" rather than a gated finding — a coordinator that only reads the Gate verdict / findings table (not the full prose report) would have missed it entirely. It was only caught because the coordinator fixed it anyway and then asked a third `plan-verifier` pass to specifically re-trace the fix. (`architecture-reviewer` → coordinator → `plan-verifier`)
- 2026-07-10: The coordinator had T-03's Process notes in hand — explicitly stating the plan's claim that `RepoIntel` doesn't declare `getTopFilesByRank`/`getCriticalPaths` was stale, and that the interface already declares both — before dispatching T-04. Rather than instructing T-04 to type against the (correct) interface, the dispatch brief said to "prefer whatever facts.ts itself expects" for consistency, which meant keeping the stale concrete-class typing. This directly produced `architecture-reviewer`'s 2 HIGH interface-segregation findings on the very next stage, costing a full fix-iterate round (implementer fix + re-audit, ~139K tokens, ~166s) the coordinator could have avoided by acting on information it already had. (coordinator → T-04 dispatch brief)
- 2026-07-19: The plan's `## Design audit` table described design/01.png's per-agent picker icon as a **"globe icon"**, so T-10 faithfully shipped `Icon.Globe` — but the actual asset is a **CPU/chip** icon, which only the user caught by eye after the build. This is the exact prose-vs-pixels failure mode CLAUDE.md's design-asset rule targets: a mockup paraphrased into audit text, then implemented from the paraphrase rather than the pixels. It reached the user specifically because the live design-fidelity screenshots were deferred (see the 2026-07-19 `.claude/INSIGHTS.md` "live design screenshots impractical in an isolated worktree" entry) — the structural JSX-vs-PNG check every UI task did instead can't catch an error that's baked into the audit text it's checking against. Lesson: when live design checks are skipped for cost, the plan's `## Design audit` prose is itself unverified against the assets — a fast coordinator pass reopening each cited `design/*.png` and eyeballing the built UI is cheaper than the user finding it. (`implementation-planner` design audit + deferred live design checks)
- 2026-07-19: T-08's nested live-browser design subagent first attached to the **WRONG worktree's dev server** — port :4000 is shared across git worktrees and was serving a *different* branch (rendered "CI Runs" instead of the new "Multi-Agent Review" nav) — and only self-corrected because it noticed the mismatched sidebar contents, then relaunched this worktree's own server on :4002. Had it not caught the mismatch, T-08 would have reported a false design PASS against unrelated code. Distinct from the 2026-07-17 orphaned-Chrome-lock near-misses: the hazard here is port-sharing across worktrees, so any live design check in an isolated worktree must first confirm *which branch* the reachable server is serving (grep for a change-specific marker on the rendered page) before trusting the screenshot. (`implementer` T-08 + nested `general-purpose`)

## Process Recommendations

- 2026-07-09: On a `failed` task-notification from an async agent, check whether the cause is a stall/API-error (as opposed to a genuine task-level failure) and attempt one `SendMessage` resume before re-dispatching from scratch — this run's resume succeeded and avoided re-incurring ~$10 and 35 minutes. (orchestrator behavior)
- 2026-07-09: Standardize future `researcher` dispatch prompts on the literal `output: compact-digest` directive from Pattern 1 (`.claude/agents/README.md:530`) instead of ad hoc "under N words" phrasing, so digest-sizing is auditable against the documented pattern. (dispatch-prompt convention)
- 2026-07-09: During `grilling`, dispatch `researcher` subagent(s) for codebase-evidence questions per the skill's own instruction rather than defaulting to direct `Grep`/`Read`/`Bash` — the skill currently has no proportionality carve-out for a trivial single-fact check, so either follow it consistently or propose (as a separate, explicitly-approved change) adding an explicit threshold to `grilling/SKILL.md` for when a direct check is acceptable. (`grilling` skill adherence)
- 2026-07-09: After `implementation-planner` returns a `**Next step:**` directive naming a mandatory handoff (e.g. run `grilling`), invoke it immediately rather than pausing to ask the requester for permission — `CLAUDE.md`'s "Mandatory handoffs — do not skip, even mid-conversation" already pre-authorizes the action; asking first only adds an avoidable round-trip for something that isn't actually discretionary. (orchestrator behavior)
- 2026-07-09: `run-plan`'s Stage 1 dispatch model gates on whole-phase completion ("A phase is complete only when every task in it reports DONE... Do not start a dependent phase early"), but within a single phase containing multiple parallel tasks, a task that depends on only ONE sibling (per its own `Depends-on` line) should be dispatched the moment that specific sibling clears — not held until every other sibling in the same phase also reports back. On the `project-context` build this cost ~3.8h of wall-clock (T-05 depended only on T-04, but wasn't dispatched until T-06 and T-07 — both stalled on an unrelated API limit — also finished). Track per-task dependency satisfaction within a phase, not phase-batch completion, when a same-phase task's `Depends-on` doesn't cover every task dispatched alongside it. (`run-plan` skill, Stage 1)
- 2026-07-09: `implementer` agents should be told explicitly not to launch their own verification command with `run_in_background` and then end their turn expecting a notification — subagents don't receive the orchestrator's task-notification mechanism, so that pattern stalls indefinitely until the coordinator notices and intervenes (observed twice in one dispatch, see Friction Points). Add to the implementer definition: run verification synchronously, or if a long-running command must be backgrounded, actively poll and read its result before ending the turn. (`implementer` agent definition)
- 2026-07-10: When a completed task's own Process notes flag a stale or incorrect claim in the plan text and identify the verified-correct alternative, apply that correction directly in the next dependent task's dispatch brief — don't default to "stay consistent with the sibling task's existing (stale) choice." Consistency with a known-wrong pattern isn't a virtue; it just relocates the fix to a later, more expensive stage (see the matching Near-Miss above). (orchestrator behavior, `run-plan` Stage 1)
- 2026-07-10: `architecture-reviewer` cannot run `git diff`/`git status` itself (Read/Glob/Grep/Skill tools only, by design) and self-reported this as a scope gap in its round-1 Process notes on the `onboarding-generator` run — it fell back to trusting the coordinator's hand-typed file list as the diff boundary. Paste actual `git status --short` (or `git diff --stat`) output into future `architecture-reviewer` dispatch prompts instead of a manually enumerated file list, to close this self-reported gap at near-zero cost. (`architecture-reviewer` dispatch-prompt convention)
- 2026-07-10: A single `implementer` task that bundles "build a full page + N section components + i18n + tests + live-browser design verification" (T-08 on `onboarding-generator`: 272K tokens, 168 tool calls, 38.2 minutes, 2 nested verification agents) is disproportionately larger than every sibling task in the same plan and became the run's entire DAG critical path almost by itself (2292.9s of a 3907.2s total wall-clock). Future plans should flag a task of this shape as expected-long upfront, or split "build" from "design-verify" into two dispatches, so a legitimately long-running dispatch doesn't read as a possible hang mid-run. (`implementation-planner` task-sizing guidance)
- 2026-07-11: Add a documented "Researcher → Spec Creator" chaining pattern to `.claude/agents/README.md` (mirroring Pattern 1's Researcher → Implementation Planner digest handoff): before a `spec-creator` dispatch that needs internal-codebase grounding (existing contracts, tables, feature-model ids, sibling components), dispatch a `researcher` subagent with `output: compact-digest` instead of the coordinator performing the exploration itself with raw `Read`/`Grep`/`Glob`/`Bash` calls. Keeps the upside observed this run (rich grounding → zero clarifying round-trips, see What Worked Well) while moving the exploration cost off the coordinator's own context and onto a subagent whose output `spec-creator` still independently re-verifies per its own Hard Rules. (process gap, `.claude/agents/README.md` chaining patterns)
- 2026-07-11: Align `implementation-planner`'s frontmatter `tools:` grant with its own body instructions — either add `Skill` (matching the "Skill loading (lazy, by domain)" section and Method step 5, both of which direct the agent to call it) or, if Skill access is deliberately withheld from this read-only planning agent, remove/rewrite those body sections so they don't instruct an unreachable action. Currently the agent self-corrects by referencing skill names from memory instead of loading actual skill content — functional this run, but a latent risk if a domain skill's guidance drifts from the planner's memorized version. (`implementation-planner` agent definition)
- 2026-07-11: When retro-ing a future `implementation-planner` or `spec-creator` dispatch, default to `deep` mode rather than in-context — both agents carry the `Agent` tool and can spawn nested `researcher`/`Explore` subagents whose cost the in-context `<usage>` block would silently exclude; this run could not confirm whether any nested dispatch occurred. (`workflow-retro` skill usage)
- 2026-07-12: When running `workflow-retro` in `deep` mode on this Windows machine, invoke `analyze-journals.cjs` via the `PowerShell` tool with a native `C:\Users\...` glob path, not the `Bash` tool's git-bash `/c/...` style — the latter resolves against the wrong drive root under Windows Node and fails with no diagnostic beyond "no readable journal files matched." (`workflow-retro` skill usage, Windows-specific)
- 2026-07-17: **A subagent has no channel to report a problem mid-task — it speaks exactly once, when its turn ends. So "why didn't it tell us?" is the wrong question; the right one is "why didn't the coordinator look?"** When a dispatch runs long or the user asks whether an agent is stuck, call **`TaskOutput`** on it before re-dispatching or taking the work over. This run the coordinator did neither: the user flagged a suspected hang twice, and the coordinator re-dispatched the same task once and then took it over — never once reading the running agent's output. `TaskOutput` would have shown attempt 2 hitting four `browser is already running` errors and actively probing for the orphaned process, which would have cut straight to the real fix (`Stop-Process` the Chrome root) instead of costing a second dead dispatch. This makes actionable the observation already logged on 2026-07-10, where a `TaskOutput` status check correctly distinguished a legitimately-long dispatch from a hang. Corollary for the third-party observer problem: a user watching an idle-looking UI has strictly less information than `TaskOutput` does — treat "is it stuck?" as a prompt to check, never as evidence. (orchestrator behavior)
- 2026-07-17: Before dispatching any agent whose task drives the browser via `chrome-devtools-mcp`, verify the environment **from the coordinator** first: one `list_pages` (is the profile lock free?) and one `curl` on the app port (is the stack already up?). Both cost seconds; skipping them cost two dead dispatches, 1732s and $5.33. Two accompanying rules: (a) never put `./scripts/dev.sh` — or any long-lived foreground process — in a subagent's task steps; confirm the stack yourself and hand the agent a finished URL. (b) When an agent driving the browser hangs, kill the orphaned profile before re-dispatching: `Get-CimInstance Win32_Process -Filter "Name='chrome.exe'" | Where-Object { $_.CommandLine -like '*chrome-devtools-mcp*' }` → `Stop-Process` the root. `rtk ps` will not show it. (orchestrator behavior, `implementer` dispatch briefs)
- 2026-07-17: `workflow-retro`'s `analyze-journals.cjs` **misattributes nested agents in its tree rendering** — this run it indented `plan-verifier`'s four `researcher` children under `architecture-reviewer`, an agent whose frontmatter grants only `Read`/`Glob`/`Grep`/`Skill` and which therefore cannot spawn a subagent at all. The indentation appears to follow list order, not parentage; every `agent-*.meta.json` carries a correct `parentAgentId` (all four read `a6442c2e5ee7013ef`). Totals are unaffected. Until the script reads `parentAgentId`, do not trust the indentation for attribution — cross-check the meta files before crediting a nested agent's cost to a parent. (`workflow-retro` analyzer script)
- 2026-07-17: **APPLIED — the four 2026-07-17 recommendations above, plus the 2026-07-09 `SendMessage`-resume one, are now promoted into the skills that load them.** Findings that live only in this file are never read before a run: nothing loads `WORKFLOW-INSIGHTS.md` except `workflow-retro` itself (for dedup), and `run-plan`/`sdd` name it only as a write destination. The recommendations that demonstrably held this run — per-task `Depends-on` dispatch, Process-notes-forward — held because someone had promoted them into `run-plan/SKILL.md`'s body. So: `TaskOutput`-before-assuming-stuck and the browser/`dev.sh` environment check are now Hard rules / Stage-1 rules in `run-plan/SKILL.md`; the `SendMessage`-resume rule (open since 2026-07-09 despite saving ~$10 and 35 min when applied, and recovering 4 infra-failed dispatches on one build) is now a Hard rule too; the `parallelism` dilution caveat is in `workflow-retro/SKILL.md`; and the parent-misattribution defect was **fixed in the script** rather than documented around — `analyze-journals.cjs` now reads `parentAgentId` and emits each parent followed by its own descendants (verified against this run's journals: the four researchers moved under `plan-verifier`, `architecture-reviewer` correctly has no children, and every total is byte-identical). **The lesson about the lessons:** a retro finding is inert until it reaches a file that gets loaded — write here to remember, promote to change behavior. (`workflow-retro` → `run-plan`, `workflow-retro` skills)
- 2026-07-17: The `parallelism` figure is diluted by everything that isn't agent compute and shouldn't be read as a dispatch-efficiency score. This run logged **0.92x overall** — implying fully serial work — while Phase 2's five concurrent implementers actually hit **3.83x** (1706.3s of span inside a 445s window, all five started within 26 seconds of each other). The denominator is total session wall-clock, which here included two user interview gaps (grilling, design-file handoff) and all coordinator time. When judging whether dispatching was actually parallel, compute it per-phase from the journals' `started` + `spanS`, not from the summary line. (`workflow-retro` metric interpretation)
- 2026-07-12: `plan-verifier`'s Stage 5 final gate was the single most expensive dispatch of the `why-risk-brief` run ($10.05 of $47.57 total, driven by 896K cache-write tokens on Opus) despite Stage 3's architecture review being a clean PASS and Stage 2's functional pass already verifying 19/20 requirements before the R11 fix-loop. Consider scoping a future Stage 5 to the requirements touched by the fix-loop plus a lighter pass on the rest when the intervening stages were clean — a deliberate tradeoff against the skill's current "full re-verification is the actual merge gate" design; only worth acting on if plan-verifier cost trends upward across future runs of comparable size, not a one-off complaint. (`run-plan` Stage 5, cost)
- 2026-07-19: **The `nav.ts` NAV-entry → `shell.json` `nav.<key>` message coupling has now bitten two separate builds** — `onboarding` (2026-07-10, `client/INSIGHTS.md:128`) and `multi-agent-review` (T-08, this run) — both surfacing as a runtime `MISSING_MESSAGE: Could not resolve 'shell.nav.<key>'` from the command palette (`useShellCommands.ts:24` does `t(\`nav.${it.key}\`)`), invisible to typecheck and to every RTL test, caught only by a live render. Root cause both times: the plan gave the nav-adding task Owned paths covering `nav.ts` but **not** `client/messages/en/shell.json`, and the task's brief framed nav labels as "plain strings, not i18n keys" — true for the `label` field, but the `key` is an implicit i18n contract. Fix forward: when `implementation-planner` (or a `/run-plan` coordinator) scopes any task that adds/renames a `NAV` entry, add `client/messages/en/shell.json` to its Owned paths and state in the brief that each nav `key` needs a matching `shell.nav.<key>` message. A logged `INSIGHTS.md` gotcha didn't prevent recurrence because it never reached the task that needed it — this is the "promote to change behavior" lesson (2026-07-17) applied to task scoping. (`implementation-planner` / `run-plan` task scoping)
- 2026-07-19: `analyze-journals.cjs --prices` reports a **systemic, severe cost undercount on any `run-plan` retro** — it prices only agents whose journal logs a real model id (`plan-verifier` and its nested `researcher`s log `claude-opus-4-8`), while **every `implementer` and `architecture-reviewer` journal tags its model `<synthetic>`**, which matches no price-map substring and prices as `n/a`. On the multi-agent-review retro this meant the tool's `cost=$12.57` covered only 4 of 23 agents; the true notional cost was **≈$169** (~13× higher). Adding a `synthetic`/`general` key to the price map does **not** fix it (the matcher still misses). The reliable path: ignore the per-agent `cost` column when implementers are present and compute from the model-agnostic **aggregate token totals** the summary line always reports correctly — `in×$5 + out×$25 + cache_read×$0.50 + cache_write×$6.25` per M at Opus 4.8 rates (confirm current rates via the `claude-api` skill first). This generalizes the 2026-07-12 one-dispatch `<synthetic>` note: it's not one flaky dispatch, it's the default tag for all non-verifier subagents, so the `--prices` total is never trustworthy for a build run. (`workflow-retro` analyzer script)
- 2026-07-19: Scope live-browser design verification to task complexity — a nav-label, single-button, or other one-line UI change should be checked structurally (JSX vs the design PNG), never by spawning a live-browser design subagent. On `multi-agent-review`, T-08 (one nav line) spent a 582s / 7.74M-cache-read nested `general-purpose` agent on a live screenshot (with a wrong-worktree near-miss), while the complex results/configure/picker tasks deferred theirs — inverting effort against risk. Add to `implementer` UI-task briefs: reserve the live design-fidelity subagent for net-new views/surfaces; a one-line addition to an existing shell gets a structural check. (`implementer` UI dispatch briefs, effort-scaling)
- 2026-07-19: In `run-plan` Stage 1, don't yield the turn at a wave/phase boundary that isn't a gate — carry consecutive implementation waves through in one continuous turn (progress summaries between waves are fine; *ending the turn* to await a nudge is the cost). On `multi-agent-review`, ending the turn after Wave 5 introduced a ~61-min idle gap before Wave 6 resumed (user had to type "продовжуй"), which by itself dragged the run's headline `parallelism` to 1.08x despite healthy 1.8–2.3x per-wave concurrency. The only legitimate mid-run stops are the ones `run-plan` already names: `DONE_WITH_CONCERNS`/`BLOCKED`/`NEEDS_CONTEXT` routing and the post-build gates. (`run-plan` Stage 1, orchestrator behavior)
